[project]
name = "logistics-service"
version = "0.1.0"
description = "Logistics microservice for shipment scheduling and tracking"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "flask-cors>=6.0.2",
    "kafka-python>=2.3.0",
]
